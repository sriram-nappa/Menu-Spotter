# React Native MenuSpotter App

The project was created using the `react-native-cli` tool:

    react-native init MenuSpotter

To run this project you need to fetch the node dependencies using the following command:

    npm install
